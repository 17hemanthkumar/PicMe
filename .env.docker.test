# Test environment variables for Docker container
# This file contains example values for testing the Docker container locally
# DO NOT use these values in production!

# Database connection string (replace with your actual Neon PostgreSQL URL)
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Flask secret key (generate a random secret for production)
FLASK_SECRET_KEY=test_secret_key_for_docker_testing_only_change_in_production

# Application port (default: 8080)
PORT=8080
